# Flowvex

<div align="center">
  <h3>智能工作流，让自动化更简单</h3>
  <p>AI-powered visual workflow automation platform</p>
</div>

## ✨ 特性

- 🎨 **可视化工作流编辑器** - 拖拽式节点编辑，直观构建复杂工作流
- 🤖 **AI Agent 集成** - 内置 AI 代理节点，支持多种大语言模型
- 🔧 **丰富的节点类型** - 网页抓取、数据处理、条件判断、循环等
- 📊 **实时执行监控** - 可视化查看工作流执行状态和结果
- 🔐 **用户认证系统** - 完整的注册、登录、权限管理
- 🚀 **高性能后端** - Rust 构建，安全高效

## 🛠️ 技术栈

### 前端
- React 18 + TypeScript
- React Flow (工作流可视化)
- Zustand (状态管理)
- TailwindCSS (样式)
- Vite (构建工具)

### 后端
- Rust + Axum (API 网关)
- PostgreSQL (数据库)
- Redis (缓存)
- JWT (认证)

### 辅助服务
- Node.js Scraper Server (网页抓取)

## 📦 快速开始

### 环境要求
- Node.js >= 18
- Rust >= 1.75
- PostgreSQL >= 14 (可选)
- Redis >= 7 (可选)

### 安装步骤

1. 克隆仓库
```bash
git clone https://github.com/ZZQ4490/flowvex.git
cd flowvex
```

2. 配置环境变量
```bash
cp .env.example .env
# 编辑 .env 文件，配置数据库和其他服务
```

3. 启动后端服务
```bash
cargo run -p api-gateway
```

4. 启动前端开发服务器
```bash
cd frontend
npm install
npm run dev
```

5. 启动 Scraper 服务 (可选)
```bash
cd scraper-server
npm install
npm start
```

6. 访问 http://localhost:3000

## 📁 项目结构

```
flowvex/
├── crates/                 # Rust 后端服务
│   ├── api-gateway/        # API 网关
│   ├── workflow-engine/    # 工作流引擎
│   ├── ai-service/         # AI 服务
│   └── ...                 # 其他微服务
├── frontend/               # React 前端
│   ├── src/
│   │   ├── components/     # UI 组件
│   │   ├── pages/          # 页面
│   │   ├── stores/         # 状态管理
│   │   ├── services/       # API 服务
│   │   └── types/          # TypeScript 类型
│   └── ...
├── scraper-server/         # Node.js 抓取服务
└── migrations/             # 数据库迁移
```

## 🎯 节点类型

| 类型 | 描述 |
|------|------|
| Trigger | 触发器节点，工作流入口 |
| AI Agent | AI 代理节点，执行智能任务 |
| Open Page | 打开网页 |
| Get Text | 获取页面文本 |
| Click | 点击元素 |
| Input | 输入文本 |
| Condition | 条件判断 |
| Loop | 循环执行 |
| Output | 输出结果 |

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
